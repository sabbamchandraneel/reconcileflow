# Database Connection (PostgreSQL - Neon / Supabase / Local PostgreSQL)
# Optional for local offline development; in-memory fallback is active if omitted
DATABASE_URL="postgresql://postgres:password@localhost:5432/financial_reconciliation?schema=public"

# Auth Session Secret
JWT_SECRET="your-random-secret-key-here-min-32-chars"

# OpenAI API Key for AI Root-Cause Auditor (gpt-4o-mini, temp: 0.1, structured JSON)
# If omitted, the deterministic domain fallback engine generates structured reasoning offline
OPENAI_API_KEY=""
